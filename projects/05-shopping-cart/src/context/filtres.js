import { createContext } from "react";


// Create a context for the filters
// This context will be used to manage the filters in the application
export const FiltresContext = createContext()

// Create a provider for the filters context
// This provider will be used to provide the filters context to the application
